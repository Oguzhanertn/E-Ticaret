@model EntityLayer.Entities.Cart
@{
    ViewBag.Title = "Buy";
    Layout = "~/Views/Shared/Layout.cshtml";
}

<div class="modal-header">
    <h2>Satın Al</h2>
</div>

<div class="container">
    @using (Html.BeginForm("Buy2", "Sales", FormMethod.Post))
    {
        <div class="modal-body">
            @Html.HiddenFor(x => x.Id)

            <div class="form-group">
                Kullanıcı Ad Soyad: @Session["Ad"] @Session["Soyad"]
            </div>

            <div class="form-group">
                Ürün Bilgisi: @Html.DisplayFor(x => x.Product.Name, new { @class = "form-control" })
            </div>

            <div class="form-group">
                Adet: @Html.DisplayFor(x => x.Quantity, new { @class = "form-control" })
            </div>


            <div class="form-group">
                Ürün Resmi <img src="~/Content/Image/@Model.Product.Image" width="80" height="115" />
            </div>

            @*<div class="form-group text-center" style="font-size:15px; color:red">*@
            <div class="form-group">
                Fiyat: @Html.DisplayFor(x => x.Price, new { @class = "form-control" })
            </div>
        </div>
        <div class="form-group">
            <label>Kart Numarası</label>
            <input type="text" />
        </div>
        <div class="form-group">
            <label>Kart Sahibinin Adı ve Soyadı</label>
            <input type="text" />
        </div>
        <div class="form-group">
            <label>Son Kullanma Tarihi</label>
            <input type="text" />
        </div>

        <div class="modal-footer" style="float:right">
            <button class="btn btn-success">Satın Al</button>
            <a href="/Cart/Index" class="btn btn-dark">İptal</a>
        </div>
    }
</div>

